# Logo assets

- `SiteSmithlogo.png` — your original upload, untouched.
- `logo.png` — same art, with the flat dark-gray background removed (chroma-keyed to real
  transparency). Kept for reference / future use (e.g. a favicon or social-share image).
- `mark.png` — just the geometric `<•>//` symbol, cropped out with the "SITESMITH DESIGN" text
  removed (that text no longer matched the site's name once it became "VenturePage"). This is
  the file `index.html` actually uses in the header/footer, next to a live text wordmark.

If you get a proper logo designed around "VenturePage" later, drop it in here as `mark.png`
(or update the `src` in `index.html` if you want to keep both around).
